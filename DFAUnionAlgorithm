# DFA Union Algorithm

To find the union of two Deterministic Finite Automata first create the Cartesian Product of the two DFAs. 
The accept states of the union will then become any state where either original state is an accept state.
